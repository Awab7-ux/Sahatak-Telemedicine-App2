# خطة تشغيل Sahatak n8n - المرحلة 1: إعداد n8n.cloud

## الخطوة 1: إنشاء حساب n8n.cloud مجاني

اتبع هذه الخطوات بالترتيب:

### 1.1 - الوصول إلى موقع n8n.cloud
- افتح متصفحك وانتقل إلى: **https://n8n.cloud**
- انقر على زر **"Sign Up"** أو **"Create Account"** (في أعلى الصفحة اليمين)

### 1.2 - ملء نموذج التسجيل
ستحتاج إلى إدخال:
- **البريد الإلكتروني**: awabpasta@gmail.com (أو بريدك الذي تفضله)
- **كلمة المرور**: اختر كلمة مرور قوية (على الأقل 8 أحرف)
- **اسم المساحة العمل** (Workspace Name): اكتب اسماً سهلاً مثل "Sahatak-Automation"

### 1.3 - تأكيد البريد الإلكتروني
- ستصل رسالة بريد إلى بريدك الإلكتروني من n8n
- افتح الرسالة وانقر على رابط **"Confirm Email"** أو **"Verify Email"**
- سيتم تحويلك تلقائياً إلى لوحة التحكم

### 1.4 - الدخول إلى مساحة العمل
بعد التأكيد، ستصل إلى صفحة n8n الرئيسية (Dashboard).

---

## الخطوة 2: الحصول على بيانات الويب هوك (Webhook URLs)

سنحتاج إلى عنوان URL فريد لكل سير عمل (workflow). ستتلقى هذه الروابط من n8n بعد إنشاء السير العمل.

### مثال على رابط Webhook:
```
https://n8n.cloud/webhook/<UNIQUE_ID>/<WORKFLOW_ID>
```

لا تقلق - سنحصل على هذه الروابط الفعلية بعد إنشاء السير العمل الخاص بنا.

---

## الخطوة 3: إعداد متغيرات البيئة (.env) في الخادم الخلفي

عندما تكون جاهزاً، ستحتاج إلى تعديل ملف `.env` في مجلد backend:

### أماكن المتغيرات في ملف `.env`:

```bash
# n8n Webhook Configuration
N8N_WEBHOOK_ENABLED=true
N8N_WEBHOOK_SECRET=your-secret-key-here-change-this

# سيتم تعبئة هذه بعد إنشاء السير العمل في n8n:
N8N_WEBHOOK_ERROR_HANDLER=https://n8n.cloud/webhook/xxx/yyy
N8N_WEBHOOK_APPOINTMENT_CREATED=https://n8n.cloud/webhook/xxx/zzz
N8N_WEBHOOK_APPOINTMENT_STATUS_CHANGED=https://n8n.cloud/webhook/xxx/aaa
N8N_WEBHOOK_DOCTOR_VERIFIED=https://n8n.cloud/webhook/xxx/bbb
N8N_WEBHOOK_AUDIT_LOG=https://n8n.cloud/webhook/xxx/ccc
N8N_WEBHOOK_NOTIFICATION=https://n8n.cloud/webhook/xxx/ddd
```

---

## الخطوة 4: ملاحظات مهمة

### 4.1 - المصادقة (Security)
- السر (Secret) يُستخدم لتوقيع كل رسالة webhook بتقنية HMAC-SHA256
- يجب أن تكون هذه القيمة طويلة وعشوائية وقوية
- **لا تشاركها مع أحد**

### 4.2 - حالة الحرية (Freedom Tier)
- الحساب المجاني في n8n.cloud يسمح بـ:
  - عدد محدود من السير العمل
  - تنفيذات محدودة شهرياً
  - يكفي للاختبار والتطوير

### 4.3 - التفعيل التدريجي
- في البداية: `N8N_WEBHOOK_ENABLED=false` - الويب هوك معطلة
- بعد إنشاء سير العمل الأول: `N8N_WEBHOOK_ENABLED=true` - تفعيل الويب هوك

---

## الخطوة 5: ماذا بعد؟

بعد إكمال هذه الخطوات:
1. أخبر المساعد بأن حساب n8n الخاص بك جاهز
2. سننشئ سير العمل **Error Handling** الأول معاً
3. سنختبره بعينة بيانات واقعية
4. ثم ننتقل إلى سير العمل التالي

---

## الأسئلة الشائعة

**س: ماذا إذا نسيت بيانات دخولك؟**
ج: استخدم خيار **"Forgot Password"** على صفحة تسجيل الدخول.

**س: هل يمكنني تغيير البريد الإلكتروني لاحقاً؟**
ج: نعم، في إعدادات الحساب (Account Settings).

**س: كم عدد سير العمل التي يمكنني إنشاؤها؟**
ج: يعتمد على الخطة. الخطة المجانية تسمح بعدة سير عمل جيدة.

**س: هل أحتاج إلى الاتصال بالإنترنت طوال الوقت؟**
ج: نعم، n8n.cloud هو خدمة سحابية - تحتاج دائماً إلى إنترنت نشط.

---

## التواصل مع الدعم

إذا واجهت مشاكل في أي خطوة:
1. تحقق من أن متصفحك مدعوم (Chrome, Firefox, Safari, Edge)
2. امسح ذاكرة المتصفح (Cache) وحاول مرة أخرى
3. تأكد من اتصالك بالإنترنت
4. جرب متصفحاً مختلفاً

لمزيد من المساعدة: [docs.n8n.io](https://docs.n8n.io)

---

**متى تكون جاهزاً؟**
أرسل لي رسالة قائلة: "تم إنشاء حساب n8n الخاص بي" وأنا سأساعدك بإنشاء سير العمل الأول!
